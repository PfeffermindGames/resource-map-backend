<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Giz Resource Map Admin Panel</title>
    <style media="screen">
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
      }
      .message {
        padding: 1rem;
        border: 1px solid grey;
        margin-bottom: 1rem;
        text-align: center;
      }
      .message.success {
        background: #a6dcf9;
      }
      .message.error {
        background: #fd9595;
      }
      h1 {
        text-align: center;
        font-size: 1.5rem;
      }
      form {
        width: 400px;
        margin: 3rem auto;
        text-align: center;
      }
      .logout {
        position: absolute;
        right: 1rem;
        top: 1rem;
      }
      .btn {
        margin-top: 3rem;
        background: #0089ff;
        border: none;
        color: white;
        padding: .4rem .8rem;
        font-size: 1rem;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h1>Resource Map Admin</h1>
    <a class="logout" href="#" onclick="window.location = window.location.href.replace(/:\/\//, '://log:out@')">
      Logout
    </a>
    <% if (req.query.error) { %>
      <div class="message error">Error uploading file</div>
    <% } else if (req.query.success) { %>
      <div class="message success">File uploaded successfully!</div>
    <% } %>

    <form action="/upload_data" method="post" enctype="multipart/form-data">
      <label>Upload Excel file:</label>
      <input type="file" name="file" required>
      <input type="submit" value="Upload" class="btn">
    </form>
  </body>
</html>
